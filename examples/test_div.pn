Restrict x,y are nat.

even_divisible() := forall x. if x is even then exists y. x/2 = y
odd_indivisible() := forall x. if x is not even then !(exists y. x/2 = y)

#assert_prop(true, even_divisible)
#assert_prop(true, odd_indivisible)
